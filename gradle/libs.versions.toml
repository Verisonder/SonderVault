[versions]
agp = "8.7.3"
kotlin = "2.0.21"
coreKtx = "1.15.0"
lifecycle = "2.8.7"
activityCompose = "1.9.3"
composeBom = "2024.12.01"
bouncycastle = "1.79"
biometric = "1.1.0"
fragment = "1.8.5"
junit = "4.13.2"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }

androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-compose-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-compose-material3 = { group = "androidx.compose.material3", name = "material3" }

# Argon2id. Called through the lightweight API, never registered as a JCE provider:
# Android ships its own Bouncy Castle repackaged under com.android.org.bouncycastle,
# and registering a second one is how provider conflicts start.
bouncycastle = { group = "org.bouncycastle", name = "bcprov-jdk18on", version.ref = "bouncycastle" }

# BiometricPrompt needs a FragmentActivity, which is why SecureActivity extends one.
androidx-biometric = { group = "androidx.biometric", name = "biometric", version.ref = "biometric" }

# Pinned deliberately. biometric 1.1.0 brings fragment 1.1.0, whose FragmentActivity
# rejects any startActivityForResult request code above 16 bits — and the modern
# activity result API generates codes above 65536 on purpose. Every launcher in the app
# throws "Can only use lower 16 bits for requestCode" until this is forced forward.
androidx-fragment = { group = "androidx.fragment", name = "fragment-ktx", version.ref = "fragment" }

junit = { group = "junit", name = "junit", version.ref = "junit" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
